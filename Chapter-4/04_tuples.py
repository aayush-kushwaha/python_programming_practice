#Creating tuples using ()
t = (1, 2, 3, 4, 5)

# t1 = () # Empty tuples
# t1 = (1) # Wrong way to declare a tuple with Single element
# t1 = (1,) # Tuple with single element 
# print(t1)

#Printing the elements of a tuple
# print(t[0])

#We cannot update the value of a tuple 
# t[0] = 34 # Tuple doesn't support item assignment # Throws error
# Tuple is immutable and its element can't be changed